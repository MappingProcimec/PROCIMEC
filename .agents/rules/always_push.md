# Regla de Control de Versiones

Siempre que se realicen cambios en el código o archivos del repositorio:
1. Ejecutar `git add -A`
2. Realizar un commit claro y descriptivo con `git commit`
3. **Realizar `git push origin main` inmediatamente sin esperar a que el usuario lo pida.**
